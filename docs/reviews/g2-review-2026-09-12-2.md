# G2 独立レビュー結果（2回目・T-203 1回目 / T-201 6回目）— 2026-09-12

orchestrator（Claude メインセッション）が memory §4 RV-020（T-203）・RV-021（T-201）として記録。reviewer サブエージェント2体（read-only・独立）の指摘。
実装担当（Codex セッション）は本指摘を修正し、`docs/t203-handoff.md` / `docs/t201-handoff.md` に「レビュー対応」表（指摘番号 / 変更ファイル:行 / RED→GREEN の実結果とコマンド・件数）を書いて再レビュー依頼すること。
レビュー時点のスナップショット: 対象ファイルの sha256 はレビュー中不変。reviewer 実測 `pytest tests/ -q` **392 passed** / ruff 指摘 0 / `alembic heads` 単一ヘッド / `make check-run-step-index` 両 DB PASS。

---

## T-203 判定: DONE 不可（P1 1 / P2 5 / P3 6）

### orchestrator の判断（先に読む）
- **P1 は「コードを直す」で確定**（agent-plan.md AE06 は変えない）。URL ガードレールは「取得・送信の意図を持つ引数」に限定し、記録系ツールの原文フィールド（`quote` / `excerpt` / `reason` / `*_raw`）は検査対象から外す。AE06 の回帰テスト（原文中に `https://…` を含む `record_question` / `record_evidence` が **成功**し、外部取得の呼出しが無いこと）を単体テストとミニ評価シナリオ（`ae06_url_in_source`）の両方に足す。
- **P2-1 / P2-2（`tool_rejected` / `guardrail_denied` が設計書に無い）は設計書への追記で閉じる**。Codex が agent-plan.md「T-203 ローカル実行の具体化」節と 04-db.md §3.2 補足の一覧に追記してよい（実装は変更不要）。追記内容を handoff に引用する。
- **P2-3（`stage_detail` が `資料 n/N` を作れない）は T-203 で直す**。T-204（ポーリング UI）が直接依存し、G2 を縦に1本通す（AD-011）ために必要。
- **P2-4（旧 `TraceRecorder` の dead code）は削除する**。`trace.py` 全体と `tools.py` との `digest_args` 二重定義を解消。削除は「無断で行わない」対象だが、**本指示で承認済み**。
- **P2-5（同一違反3回のループが既定方針では到達不能）は記録のみ**。コード修正不要。handoff の「残件」に明記。

### P1
- **[P1] `backend/app/agent/hooks.py:69`（+ `definition.py:70`, `runner.py:94`）— URL ガードレールの過剰遮断で AE06 が成立しない。**
  `_contains_blocked_pattern(tool_input)` が全引数を再帰走査するため、原文に `http(s)://` が現れるだけで `record_evidence.evidence.quote` / `record_question.question.reason` / `record_source_inventory.entries[].excerpt` が deny になり、`tools.py:74` → `runner.py:94` `RunResult("failed", …, "tool_rejected")` で**実行全体が中断**する（reviewer が実機で再現）。
  正: agent-plan.md AE06「期待＝**成功**。送信を実行せず当該記述を業務条件・確認事項として抽出するに留める」。「外部取得の禁止」を「原表記の記録禁止」まで拡大し、別ガードレール（原表記・出典の書き換え禁止）を侵している。
  現ダミー方針では URL 行が `ROW` 正規表現に一致せず発火しないため、**13ケースのミニ評価では検出できない**。
  → 修正方針は上記「orchestrator の判断」。

### P2
- **[P2-1] `backend/app/agent/runner.py:94`** — 任意のツールエラーで実行全体を `failed` にする経路（`detail="tool_rejected"`）が agent-plan「失敗（中断）」①②に無く、04-db.md:939 の `stage_detail` 一覧にも無い。→ agent-plan「T-203 ローカル実行の具体化」に停止理由の内訳として明記し、04-db §3.2 補足の一覧へ `tool_rejected` / `local_dummy_unsupported` / `validation_unresolved` を追記。
- **[P2-2] `backend/app/agent/tools.py:69`** — 管理イベント名 `guardrail_denied` が agent-plan / 04-db.md:938 のどちらにも無い（04-db.md:459「ツール一覧に無い名前は記録しない」）。→ 両設計書に管理イベントとして1行追記。
- **[P2-3] `backend/app/repositories/agent_tool_repository.py:281-282`** — `stage_detail` が `{"completedTools": run.turns}` で、05-api-ipo.md:298「段階の内訳（`資料 2/5` 等の材料）」・agent-plan Part 1「資料 n/N を読取中」を作れない。同一列に JSON 文字列と固定診断コードが混在。→ 読取段階は `{"documentsRead": x, "documentsTotal": N}` 相当を入れる（`list_case_documents` の件数と `read_*` の進捗から算出）。
- **[P2-4] `backend/app/agent/trace.py`（全体 116 行）** — 旧 `TraceRecorder` が参照 0 の dead code。docstring「すべての実行はここを通す」が残り、第2のトレース系統として復活しうる。`digest_args` が `tools.py` と二重定義。→ 削除（承認済み）。
- **[P2-5] `backend/app/agent/runner.py:100-112` / `local_policy.py:198`** — `local_dummy_policy` は違反1件で即 `validation_unresolved` 停止のため、`validation_repetitions` は既定経路で常に1。`validation_loop` 評価は `scripts/evaluate_local_agent.py:53` の合成方針 `repeat_validation` でのみ通っている。「自己修復→再検証→3回で中断」（異常系 B）は未稼働。→ 記録のみ。実モデル接続時に方針側へ自己修復ループを実装する前提を handoff・memory に残す。

### P3
- **[P3-1] `tools.py:147-154`** — `ping` が `@tool` 定義のまま未登録の dead code。`SYSTEM_PROMPT` / `hooks.build_hooks()` / `agent_server` も未使用。実モデル接続時に `ClaudeAgentOptions(hooks=build_hooks(), allowed_tools=...)` へ渡す必要をコメントで残す。
- **[P3-2] `tools.py:74`** — 未登録ツール拒否と外部 URL 拒否が同じ `E_VALIDATION_FAILED` / `guardrail_denied` に丸められ、発火理由を区別できない。→ `E_TOOL_NOT_REGISTERED` / `E_EXTERNAL_LINK_BLOCKED` を `observation.code` に。
- **[P3-3] `definition.py:47` `CANCEL_CLEANUP_S` と `jobs.py:10` `CANCEL_GRACE_S`** — 同義定数2箇所（CV-015）。別値を許すならコメント。
- **[P3-4] `agent_tool_repository.py:258-262`** — `read_email` が常に `locator="email:*"` で全 readable パートを走査済みにする。実モデルが一部パートを無視しても未走査が検出されない（要確認・記録のみ）。
- **[P3-5] `tools.py:76`** — hook に `HookContext` でなく `{"signal": None}` の素 dict。
- **[P3-6] handoff** — 変異試験が3件のみ。ガードレール系はテストの変異強度を上げる（今回の P1 は単体でも評価でも未検出だった）。

### ミニ評価の突き合わせ（CLAUDE.md 実装フロー⑤・reviewer 代行）
- `normal`（run_id=1, 12 イベント）: 使用ツールは全て agent-plan ツール一覧 13 本の範囲内。`get_rules → list_case_documents → read_document(body:1) → record_case_header → propose_items → record_evidence×2 → record_source_inventory → validate_draft(違反0) → finalize_draft → job_finish(completed)` で IPO 拡張表ステップ 1〜10 と対応。完了は `DraftService.finalize` と `RunRepository.finish` の二重判定で自己申告ではない。
- 停止系 9 シナリオの `stopReason` は agent-plan:73 / 04-db.md:444 の 9 語彙と一致。新語彙の後付け無し。全て `versionId=null`・`job_interrupted` 1回のみ。
- 漏洩検査: 13 シナリオ全 JSONL を `150 MT` / `synthetic-secret-command` / `casing` / `Kind:` / `Qty:` / `Traceback` / `example.invalid` で grep → ヒット 0。
- トレースを通らない経路: `query()` / SDK 実行呼出しは app 配下に無し。起動は `RunService.start → RunDispatcher → jobs.start_agent_job` の1経路。GET は `RunRepository.progress`（read のみ）で副作用無し。
- **未検証**: AE06 本来のケース（原文中 URL を記録する成功パス）。P1 修正時にシナリオを追加する。

### 良い点（維持）
- TODO-006「ガードレール未接続」は解消。hook を呼出し境界で必須化し、`mcp__app__` 限定を全ツール名へ拡張。ContextVar 未束縛時の `RuntimeError` で handler 直接呼出しを構造的に防止。
- D03: `get_rules` が `conversion_enabled=True` を拒否、`Arguments(extra="forbid", strict=True)` が換算列を弾く。D05: 外部送信経路ゼロ、ダミー対応範囲を設計書に先に書いてから実装。
- 層配置: `app/agent/` は `app/api` を import せず、ツールは service → repository。CV-015 の SSOT テストが新規ファイルも自動検査。
- エージェントループの単体テストを書いていない。`test_parallel_steps_are_serialized_by_run_lock` は実 PostgreSQL で走る（LN-014 非抵触）。

---

## T-201 判定: DONE 可（条件付き。P1 0 / P2 1 / P3 5）

RV-017 残 P2（実 DB に索引が無い）は**実測で閉じた**（両 DB `alembic current` = `add_run_step_locator_index`、`check-run-step-index` 両 PASS、`upgrade --sql` で `CREATE INDEX IF NOT EXISTS` 生成を確認、t201_artifacts / t202_run_metadata 未編集）。

### orchestrator の判断
- **P2 は本ラウンドで直す**（Makefile 1行）。T-203 の修正と同じ handoff サイクルで対応し、それをもって T-201 DONE とする。

### P2
- **[P2] `Makefile:71-73`** — `check-run-step-index` が `check` / `check-be` に含まれておらず、pytest 側の migration 検査は除去済み、`tests/t201/conftest.py:71` は `create_all` のため**適用差分を原理的に検出できない**。「migration 経由で索引が作られること」を自動で守るものがゼロ（CV-016 の精神に反する）。→ `check-be` に `check-run-step-index` を追加。より良いのは `tests/integration/` にテスト DB の `pg_indexes` を読むテストを1本置く（`be-test` に入るので自動化）。

### P3
- **[P3-1] `add_run_step_locator_index.py:27-29`** — downgrade が常に `RuntimeError`。索引の所有者は t201_artifacts 側なので正しい downgrade は「何もしない」＋コメント。
- **[P3-2] `t201_artifacts.py:29` と新リビジョン:15** — 同一索引の二重定義（新規 DB は前者、既存 DB は後者が作る）。LN-018 優先の判断は妥当。memory に事実を残す（LN-025）。
- **[P3-3] `check_t201_postgres.py:58-72`** — 列の ASC/DESC・opclass 未検査（任意）。
- **[P3-4] `check_t201_postgres.py:80-96`** — `octg_postgres` / `octg_db` / `octg_test` を直書き。Makefile の `TEST_DB` と二重化。→ 環境変数で渡す。
- **[P3-5] `check_t201_postgres.py:136-141`** — `--index-only` が合成検査をスキップするだけで実 DB 検査は常に走る。既定を合成のみ・`--with-live-index` で追加、が素直（任意）。

### 良い点（維持）
- 適用済みリビジョン無編集＋新リビジョン `if_not_exists=True` の収束手順は LN-018 の模範。実効性を offline SQL 生成で確認できる形。
- 実 DB 検査が `indisvalid` / `indisready` / 非 UNIQUE / 非部分 / 非式 / btree / 列順を見ており false green をほぼ潰している。`BEGIN READ ONLY … ROLLBACK`、接続情報を出力しない。
- テスト件数減・skip/xfail 無し（tests/t201 104 passed）。
