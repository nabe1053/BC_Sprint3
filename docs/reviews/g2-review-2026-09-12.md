# G2（T-201 / T-202）独立レビュー結果 — 2026-09-12

orchestrator（Claude メインセッション）が memory §4 RV-015（T-202）・RV-016（T-201）として記録。 reviewer サブエージェント2体を並行起動して得た指摘。
実装担当（Codex セッション）はこの指摘を修正し、修正後に再レビューを受けること（CLAUDE.md 憲法6）。
レビュー時点のスナップショット: tests/t202 60 passed（handoff の 55 は古い）、tests/t201 77 passed。
レビュー中も作業ツリーが更新されていた（11:43 JST）。行番号はその時点のもの。

---

## T-202 判定: DONE 不可（P1 2 / P2 9 / P3 9）

### P1（DONE 前に必須）

- **P1-1 全体回帰が red（44 ERROR）**。`pytest tests/ -q` → 262 passed / 44 errors、全件
  `UndefinedColumn: agent_run_steps.trace_event does not exist`。`app/models/agent_runs.py:109` で ORM に
  列を足したが `t202_run_metadata` が octg_test / 開発 DB に未適用。`tests/conftest.py:36-46` が
  `Base.metadata` を TRUNCATE するため既存統合テストが全滅する。
  → `alembic upgrade head` をテスト DB・開発 DB に適用し `pytest tests/` を green にする。
  （参考: `tests/unit/test_api_path_separation.py` は単独 3 passed。#12〜#21 の分離違反なし）
- **P1-2 停止閾値・モデル識別子が `definition.py` の外に複製**（agent-development.md §1 違反）。
  `app/domain/run_types.py:19-23` の `RunLimits(40/900/60/960)`、`app/repositories/run_repository.py:147` の
  `model="mock-fixed-v2"` 直書き。正は `app/agent/definition.py`（MAX_TURNS / *_TIMEOUT_S / DUMMY_MODEL_ID）。
  → `RunLimits` の既定値と `model` を definition から取る（repository へは service 経由で注入）。

### P2（修正推奨）

- **P2-1 GET #13 が毎回 DB 書込＋FOR UPDATE＋JSONL 全書換**。`run_repository.py:302-303` → `233-234`。
  ポーリングが実行中 run 行を排他ロックし `finish()` と競合。→ 再エクスポートは未出力 trace_event が
  あるときのみ／終端時のみに限定。
- **P2-2 ディスク書込失敗が GET 経由で実行中 run を failed にする**。`run_repository.py:250-260`。
  一時的なディスク満杯＋ポーリングで生きている run が終端され、以後ワーカーの書込が `E_RUN_NOT_ACTIVE`。
  → running は failed にせず `trace_write_failed` の記録のみ。終端処理中とポーリング中を区別。
- **P2-3 DraftError→HTTP status 対応表の二重化**。`routes_t202.py:11-22` vs `app/api/errors.py:45-54`
  （T-102 決定8「1箇所に集約」）。→ `DraftError` を `DomainError` 系に統合し表を1つに。
- **P2-4 業務エラーコードを Pydantic のメッセージ文字列から復元**。`routes_t202.py:46-54, 85-88`
  （`"Value error, " + code` の parse、フィールド名ヒューリスティック）。Pydantic 更新で全コードが
  `E_REQUEST_INVALID` に退化する。→ `PydanticCustomError(code, ...)` を使い `error["type"]` で取る。
- **P2-5 保管ファイル検証の二重管理（T-102 指摘の再発）**。`run_input_files.py:42-54` が
  `document_storage.py:12-15` の `resolve_readable_path()` を使わず独自実装。`STORAGE_ROOT` 既定が
  相対パスのため CWD 依存で `size()` が None → P2-6 で全 run 起動不能になり得る。
- **P2-6 #12 で原本欠損時に `E_NOT_FOUND`(404) — 契約外**。`run_service.py:29-32`。05-api-ipo 3.1 の
  エラー表に無い。抽出済み `document_pages` があれば生成可能でもある。→ 上限判定スキップにするか、
  止めるなら専用コードを 05 §6 に追加。
- **P2-7 新規エラーコードが 05-api-ipo 6章（SSOT）に無い**。`E_REQUEST_INVALID` / `E_JOB_START_FAILED` /
  `E_RUN_NOT_ACTIVE`、および `tool_name='job_trace_failure'`。→ 6章の表と補足に追記。
- **P2-8 `jobs._execute` がワーカー例外を握りつぶす**。`jobs.py:48-49, 28-31`。型名すらログに残らない。
  再試行 `51-57` はスリープ無しの即時3連打。→ `type(exc).__name__` をログ、指数バックオフ。
- **P2-9 外側タイムアウトでワーカー cancel を待たない**。`jobs.py:39-42`。終端後もワーカーが版に
  書き続け得る（現状は `E_RUN_NOT_ACTIVE` が防波堤）。→ 短い猶予で `asyncio.wait` するか、
  T-203 ワーカー契約に「cancel 後は書かない」を明記。

### P3（任意 / 記録）

- P3-1 `run_repository.py:181-188` の `seq=max+1` は run ロック外だと衝突（T-203 のツール step 採番方式を決める）
- P3-2 `stage_detail` に内部トークン（`worker_failed` 等）を入れている。04-db.md:442 は「件数の材料」
- P3-3 `app/agent/runner.py` / `trace.py` / `hooks.py` が孤児化。旧 TraceRecorder と新 RunTraceStore の
  2系統が同居。**T-203 前に正を決め、ガードレール（hooks / disallowed_tools）の差込口を `RunDispatcher` に用意する**
- P3-4 新 `trace_event` に decision / observation の置き場が無い（IPO 拡張表との同型性は T-203 で拡張要）
- P3-5 `t202_run_metadata.py:179` downgrade が RuntimeError（方針として memory に残す）
- P3-6 `check_t202_postgres.py` は合成テーブル検証で実スキーマ適用の証明にならない（連鎖自体は正しい）
- P3-7 `test_run_api.py:94-95` の 404 assert は agent ルーター未 mount なので無意味。
  `test_api_path_separation.py` の `UI_ONLY_SEGMENTS` に `agent-runs` を足す
- P3-8 SQLite テストは `with_for_update()` を検証しない。並行性の根拠は check_t202_postgres の4件のみ
- P3-9 `schemas_drafts.py:68,105` の `list[dict]` / `dict[str,int]`（orval 型が緩む。`rejected` は常に空）、
  `ruleVersion=current` 非対応、`draft_repository.py:78-80` ruff format 未適用、
  `orval.t202.config.ts` は G1 マージ後に削除して 1 本に戻す（CV-013）

### 良かった点（維持）
実行の型（endpoints→RunService→RunDispatcher→jobs）、パス分離、`RunLimits.__post_init__` による
大小関係の構造的強制、トレースに本文・例外・秘密を書かない設計とそのテスト、`finish()` の二重終端防止、
`draft_not_finalized` で自己申告 completed を拒否する判定。

---

## T-201 判定: 条件付き DONE 可（P1 0 / P2 4 / P3 8。全体回帰保留を除く）

T-202 による T-201 テスト変更（`test_draft_repository.py:252` の outcome 修正）は**テスト改竄ではない**
（判定軸は run_id であり検出力は落ちない）。

### P2
- **P2-1 Repository の「走査済み」「相殺」の正常系が 0 本**。`test_draft_repository.py:240-292` は否定側のみ。
  `draft_repository.py:289` の `add` を消しても `302-306` の集合を空にしても 77 passed のまま通り得る。
  → `read_document/ok` で scanned に入る／intake issue で excused に入る／部分 `email:postscript:2` の3本を追加。
- **P2-2 明細の明示状態→根拠必須が 9 項目×2 状態のうち `qty×tba` 1本のみ**。`draft_validation.py:55-67`。
  案件情報側はパラメタ化済みで明細側だけ粒度が落ちている（CV-014 / LN-002）。→ 同型にパラメタ化。
- **P2-3 版行ロック直列化がどのテストでも未検証**。SQLite は `with_for_update()` を出力しない。
  `check_t201_postgres.py` にも FOR UPDATE 無し。`test_every_write_rejects_finalized_version` は
  `populate_existing` を外しても通る。→ PostgreSQL 2セッション確認を足すか、docstring と memory に明記。
- **P2-4 04-db.md:861 の索引 `agent_run_steps(document_id, locator)` が未作成**。唯一の利用者が
  `snapshot()`（`draft_repository.py:271-277`）。→ migration に追加するか設計から削って理由を書く。

### P3
`source` 疑似フィールドが冗長かつ語彙未定義（`draft_validation.py:87-89`）／`draft_repository.py` ruff format 未適用／
`E_RUN_NOT_ACTIVE` の回帰テストが tests/t201 に無い／空 page を readable から除外する判断・`not_stated` を
明示状態から除外する判断が 04-db.md 未記載／両端 `missing_unit` の detail が `end_a.od` でない／
`getattr(snapshot,"ends",[])` の不要な防御／`email_parts` に `UNIQUE(document_id, part_role, seq)` が無い。
