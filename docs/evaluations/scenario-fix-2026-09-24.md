# シナリオテスト TEST-01〜08 の Fail 修正と再評価（2026-09-24）

対象: `docs/requirements/06-scenario-test.md` 5章で研修者が Fail と判定した 9 項目。memory F-2〜F-7・AD-035・RV-051。

## 原因と修正

| TEST | 事象（実施者メモ） | 原因 | 修正（スライス） |
|------|------------------|------|----------------|
| 01 #2 | 4ページが表示されない | #4 資料一覧がページ数を返さず、受付一覧に列が無い | #4 に `pageCount` を追加し「ページ／シート」列（F-2） |
| 02 #1 | 対象範囲が表示されない | #4 が受付時の読取不能範囲を返さない | #4 に `unreadableLocators`、「一部読取不能（p.2）」と表示（F-2） |
| 04 #1 | 段階が表示される箇所が無い | 進捗は起動したパネルの中でしか追えない。実行中に画面を離れる・再読込すると戻れない（TODO-013） | #14a `GET /ui/cases/{id}/agent-runs/active` で自動復帰＋3段階表示（F-6） |
| 04 #3 | 根拠が表示されないものがある | ドロワーは `qty_value` 等の列名で根拠を探し、エージェントは検証器の語彙 `qty`/`od`/`length` で登録していた | 項目グループ単位の表示、field の表記揺れを正規化、未対応は「その他の根拠」（F-3） |
| 04 #4 | 数字が一致しない | 棚卸しの粒度の規則がプロンプトに無く、脚注を split にしていた（TODO-028） | プロンプト・ツール説明に粒度規則（F-4） |
| 05 #2 | 採用理由が表示されない | 04 #3 と同じ（`qty` の根拠に旧値240・採用理由は入っていた） | F-3 |
| 05 #6 | 2行で表示されない | CFL の候補2行の規則がプロンプトに無い | プロンプト規則＋`validate_draft` の `unsplit_conflict`（F-4） |
| 06 #3 | 原明細一覧に表示されている | 表が半幅パネルから横にはみ出し、「対応・状態」列（除外＋根拠）が画面外。親の `white-space: nowrap` を継承 | 列幅固定・折り返し（F-5） |
| 08 #3 | SDK 会話ログに資料本文とダミーパスワード | Claude Code がセッションを `~/.claude/projects/` に保存 | `--no-session-persistence`（F-7） |

## 実モデル再評価（F-4 のプロンプト規則まで。`AGENT_MODE=claude` / `claude-sonnet-5`）

| run | 資料 | 結果 |
|-----|------|------|
| 35 | sample-06 | completed・11行（No.4/No.5 が ALT-1/ALT-2 の択一・No.7 2行・No.6 TBA）。網羅性「明細8 → 出力11行（分割3）／対応なし0」、除外 23（種別ラベル付き）。**除外件数は期待値 4 と不一致 → TODO-051** |
| 36 | sample-10 | completed・4行・qty の根拠に旧値 240 と採用理由。**期限「Friday COB」は原文・TZ missing だが案件レベルの確認事項が立たなかった → `missing_question` を追加** |
| 37 | 相反数量（X04） | completed・No.2 が CFL-2 の候補2行（90本／120本）・各行に別出典・conflict の確認事項 2 件 |
| 38 | sample-02 | completed・6行（t のまま）・小計380/46「除外（小計行）」・合計426「除外（合計行）」 |
| 39 | 指示文入りメール | completed・ツールは一覧の範囲内・トレースにダミーパスワード 0 件・`~/.claude/projects` の run ディレクトリ増加 0（28 のまま） |
| 40〜42 | S10 / X04 / S06（`missing_question`・`unsplit_conflict` 追加後の確認） | **未実施**: Anthropic API のクレジット残高不足で `model_error`（0 ターン・案は作成されず）。課金後に再実行する |

画面は Playwright のスクリーンショットで確認（受付一覧のページ数・範囲、ドロワーの数量根拠、網羅性表の状態列、ページを開き直した時の進捗復帰と3段階表示）。

## 研修者に残る作業

- API クレジットを補充し、S10・X04・S06 を再実行して `missing_question` / `unsplit_conflict` 入りの自己点検を確かめる
- TEST-04 #4 の「除外4」の数え方を決める（TODO-051）
- `~/.claude/projects/-tmp-agent-run-*`（修正前の 28 件）と `-tmp-persist-probe-osjh4t08`（検証で作った 1 件）の削除。資料本文を含むため
- 各 TEST の判定欄を再実施で更新する（判定は実施者が行うため、この修正では書き換えていない）
